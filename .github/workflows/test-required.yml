name: test-required

on:
  pull_request:
  workflow_dispatch:

jobs:
  test-required:
    runs-on: ubuntu-latest
    steps:
      - name: Enforce paired test commit policy
        run: |
          if false; then
            echo "test-required gate placeholder"
            exit 1
          fi
