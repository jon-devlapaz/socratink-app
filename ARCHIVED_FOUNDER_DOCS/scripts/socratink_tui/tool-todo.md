# Socratink TUI Todo

## Provisional Map Visibility

- Show a compact provisional route map immediately after `[Route]`.
- Include `core_thesis`, backbone principles, clusters, drillable subnodes, and active first target.
- Keep the copy clear that the map is a hypothesis, not graph truth.
- Preserve Generation Before Recognition: do not expose hidden mechanism answers before the cold attempt.
- Log the displayed map shape in `session.json` so founder dogfood runs can compare what was shown with what was drilled.
