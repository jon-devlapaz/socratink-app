# Tooling archive

Frozen tool/IDE configs. Each subdir is self-contained — restore by `git mv`-ing it back to the repo root if you start using the tool again.

| Item | Archived | Origin | Delete trigger |
|------|----------|--------|----------------|
| `kiro/` | 2026-04-26 | `.kiro/` at repo root — config for the Kiro IDE (`settings/mcp.json`, `steering/code-review-graph.md`) | Delete if Kiro is not back in the workflow within 90 days |
