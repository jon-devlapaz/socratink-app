#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
ENV_FILE="${SOCRATINK_TUI_ENV_FILE:-scripts/socratink_tui/.env}"
if [[ -f "$ENV_FILE" ]]; then
  set -a
  # shellcheck source=/dev/null
  . "$ENV_FILE"
  set +a
fi
exec node --no-warnings scripts/socratink_tui/app.mjs "$@"
