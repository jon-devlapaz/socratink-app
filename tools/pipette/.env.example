# tools/pipette/.env.example

# Absolute path to the gemini CLI binary. Defaults to /opt/homebrew/bin/gemini
# PIPETTE_GEMINI_BIN=/opt/homebrew/bin/gemini

# Path to the pipeline lockfile. Usually resolved automatically via git.
# PIPETTE_LOCK_PATH=
