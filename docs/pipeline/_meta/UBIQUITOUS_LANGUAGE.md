# Ubiquitous Language

Project DDD glossary. Updated by Step 1.5 of every `/pipette` run. Per-feature deltas live in each run's `01b-glossary-delta.md`.

(empty — first `/pipette` run populates this)
