# Development / test dependencies. Not bundled into the Vercel function.
# Install with: pip install -r requirements-dev.txt && playwright install chromium
#
# Source-of-truth inputs live in `requirements-dev.in`.
# Pinned lock output lives in `requirements-dev.lock`.

-r requirements-dev.lock

# Browser smoke suite (tests/e2e/)
pytest-playwright>=0.5.0
playwright>=1.45.0

# /api/health probing in test_smoke.py
requests>=2.31.0

# tools/pipette internal dev tooling (cli, subagent_stop, e2e harness)
pydantic>=2.0
pyyaml>=6.0
